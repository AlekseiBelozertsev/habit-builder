const homeIcon = require("./home.svg").default;
const likedIcon = require("./liked.svg").default;
const searchIcon = require("./search.svg").default;
const awardIcon = require("./award.svg").default;

export { homeIcon, likedIcon, awardIcon, searchIcon };
